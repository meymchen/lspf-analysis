function add(a, b) {
    return a + b;
}

function classify(a, b, c, d) {
    let total = 0;
    if (a > 1) {
        if (b > 2) {
            if (c > 3) {
                total = a + b + c + d;
            }
        }
    }
    return total;
}
