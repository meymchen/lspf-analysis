def add(a, b):
    return a + b


def classify(a, b, c, d):
    total = 0
    if a > 1:
        if b > 2:
            if c > 3:
                if d > 4:
                    total = a + b + c + d
    return total
